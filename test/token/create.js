
const chai = require('chai');
const sinon = require('sinon');
const should = chai.should();



const Token = require('../../app/model/token.js');


let sandbox = null;

describe('Feature: Create Authorization Token', () => {

	// beforeEach(() => {
	//     sandbox = sinon.sandbox.create();
	// });

	// afterEach(() => {
 //   		sandbox.restore();
	// });

	describe('Scenario: User request an Authorization Token', () => {

		it ('Given that the user update a client', () => {
			
		});
	});
});
